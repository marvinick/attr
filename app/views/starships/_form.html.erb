<%= form_for(starship) do |f| %>
  <% if starship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(starship.errors.count, "error") %> prohibited this product from being saved:</h2>
      <ul>
      <%  starship.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.fields_for :crew_members do |crew_members| %>
      <%= render 'crew_member_fields', f: crew_members %>
    <% end %>
    <div class="add-more-crew">
      <%= link_to_add_association 'Add Another Crew Member', f, :crew_members %>
    </div>
  </div>  

    <div class="actions">
      <%= f.submit %>
    </div>

    </ul>
<% end %>
